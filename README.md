# saturn-backend

Backend minimal en Go usando Gin + GORM + JWT y PostgreSQL.

## Pasos rápidos

1. Copia `.env.example` a `.env` y ajusta valores.
2. Levanta con Docker:

```bash
docker-compose up --build
```
